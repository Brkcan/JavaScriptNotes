function writeln (n)
{
    console.log(n)
}

function main ()
{
    let val = parseInt(Math.random() * 100000)
    writeln(val)
r
    Number.prototype.count = function () {
        return val === 0 ? 1 : parseInt(Math.log10(Math.abs(val)) + 1)
    }
    writeln(`Basamak Sayisi : ${val.count()}`)
}

main ()
